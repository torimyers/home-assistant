---
# Add support for scenes
#
# https://www.home-assistant.io/integrations/scene
#

# No YAML scenes are defined — nothing in this repo references a scene.* entity.
# Scenes that do exist are UI-created and live in .storage.
# To add YAML scenes: create a /scenes directory and uncomment the line below.
# scene: !include_dir_named ../scenes
